"use client";

/**
 * KeyTakeawaysEnhancer
 *
 * No longer needed — Key Takeaways sections are identified by H6SectionParser
 * and styled purely via CSS child selectors on .key-takeaways-section.
 * Kept as a no-op for backward compatibility with page imports.
 */
export default function KeyTakeawaysEnhancer() {
  return null;
}
