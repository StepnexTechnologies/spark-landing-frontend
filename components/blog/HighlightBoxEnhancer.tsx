"use client";

/**
 * HighlightBoxEnhancer
 *
 * No longer needed — Highlight Box sections are identified by H6SectionParser
 * and styled purely via CSS on .highlight-box-section.
 * Kept as a no-op for backward compatibility with page imports.
 */
export default function HighlightBoxEnhancer() {
  return null;
}
